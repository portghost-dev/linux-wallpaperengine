#pragma once

namespace WallpaperEngine::Render::OverlayLabel {

/** Draw the label (if configured) into the current viewport. GL context current. */
void draw (int viewportWidth, int viewportHeight);
}
